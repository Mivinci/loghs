package json

// Encoder empty struct
type Encoder struct{}

// AppendKey appends a new key to the output
// func (e Encoder) AppendKey(buf []byte, key string) []byte {
// 	if buf[len(buf)-1] != '{' {
// 		buf = append(buf, ',')
// 	}
// 	return append()
// }
